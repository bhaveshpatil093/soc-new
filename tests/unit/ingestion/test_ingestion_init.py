"""Tests for the ingestion module."""

def test_ingestion_scaffold() -> None:
    """Empty test to prove test discovery works."""
    import tads.ingestion
    assert tads.ingestion.__name__ == "tads.ingestion"
