"""Tests for the baselines module."""

def test_baselines_scaffold() -> None:
    """Empty test to prove test discovery works."""
    import tads.baselines
    assert tads.baselines.__name__ == "tads.baselines"
